---
plan: "03-02"
phase: 3
wave: 2
depends_on: ["03-01"]
files_modified:
  - "puma-frontend/src/screens/Screen3Questions/Screen3Questions.tsx"
  - "puma-frontend/src/screens/Screen3Questions/PrioritySlider.tsx"
requirements:
  - CTX-05
  - UI-03
---

# Plan 03-02: Priority Slider & Final Navigation

## Objective
Build the Step 5 priority slider (Comfort ↔ Performance) with a custom-styled range input, visual labels that respond to slider position, and the final CTA "See Your Match →" that navigates to `/match`. Ensure the full 5-step flow works end-to-end with back navigation, data persistence, and smooth transitions.

## Tasks

<task id="03-02-T1">
<title>Create PrioritySlider component</title>
<action>
Create `puma-frontend/src/screens/Screen3Questions/PrioritySlider.tsx`.
Custom range slider (input type="range") styled with CSS to match the gold accent theme:
- Track: dark bg with gradient accent fill
- Thumb: gold circle with glow
- Labels: "Comfort" on left, "Performance" on right, with dynamic "Balanced" label in center
- Below the slider: show the current reading as text ("Comfort-Focused", "Balanced", "Performance-Focused")
- The value maps to `priorityScore` (0–100)
- Derive `priority`: 0–33 = 'comfort', 34–66 = 'balanced', 67–100 = 'performance'

Wire to `useQuestionnaire` hook's `select` method for `priorityScore` and `priority`.
</action>
</task>

<task id="03-02-T2">
<title>Wire Step 5 into questionnaire and add final CTA</title>
<action>
In `Screen3Questions.tsx`, add Step 4 (index 4) rendering the PrioritySlider.
This step does NOT auto-advance — instead shows an explicit "See Your Match →" button below the slider.
On click: navigate to `/match`.

Ensure:
- Back button on Step 5 returns to Step 4 (Climate)
- All 5 context values are set in Zustand before navigation
- The slider defaults to center (50 = Balanced) if user hasn't moved it
- Verify the full step flow from the foot scan result CTA through all 5 steps

Add smooth exit animation when navigating to /match.
</action>
</task>
