---
plan: "02-03"
phase: 2
wave: 3
depends_on: ["02-02"]
files_modified:
  - "puma-frontend/src/screens/Screen2FootScan/Screen2FootScan.tsx"
  - "puma-frontend/src/design/animations.ts"
requirements:
  - UI-02
---

# Plan 02-03: Cinematic Scanning UI

## Objective
Integrate the live feed, the deterministic outputs, and paint a cinematic overlay on top of it. This includes the scanning beam, the SVG mesh outline that scales with the foot, and the final layout transitions before navigating to Phase 3.

## Tasks

<task id="02-03-T1">
<title>Add Scanning VFX primitives</title>
<action>
Inside `Screen2FootScan.tsx`, build a container laid `<div className="absolute inset-0">` directly over the webcam.
- If status is `aligning`, pulse a dashed SVG foot outline.
- If status is `scanning`, animate a CSS-gradient "laser beam" moving from top to bottom, filling the outline.
- If status is `analyzing`, show a spinning concentric ring component masking the foot while text reads "Generating Biomechanical Map".
</action>
</task>

<task id="02-03-T2">
<title>Wire navigation to Screen 3</title>
<action>
Ensure the `ScanResults` card contains the Call to Action `Continue to Reality Questions ->`, attached to `navigate('/questions')`.
Check that entering and exiting the screen smoothly transitions.
</action>
</task>
