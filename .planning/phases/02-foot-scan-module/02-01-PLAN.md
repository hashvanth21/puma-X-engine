---
plan: "02-01"
phase: 2
wave: 1
depends_on: ["01-03"]
files_modified:
  - "puma-frontend/package.json"
  - "puma-frontend/src/hooks/useScanner.ts"
  - "puma-frontend/src/screens/Screen2FootScan/Screen2FootScan.tsx"
requirements:
  - SCAN-01
---

# Plan 02-01: Camera Infrastructure & State Machine

## Objective
Establish the live webcam stream and the foundational deterministic state machine that guides the scan flow without yet painting the high-fidelity UI overlay.

## Tasks

<task id="02-01-T1">
<title>Install tracking library and React Webcam</title>
<action>
Execute `npm install react-webcam` and `npm install lucide-react` (for iconography) inside the `puma-frontend` directory.
</action>
</task>

<task id="02-01-T2">
<title>Create useScanner hook state machine</title>
<action>
Create `puma-frontend/src/hooks/useScanner.ts`. 
This hooks outputs the current state of the scanning sequence: 'idle' | 'aligning' | 'scanning' | 'analyzing' | 'complete'.
```typescript
import { useState, useEffect } from 'react';

export type ScanStatus = 'idle' | 'aligning' | 'scanning' | 'analyzing' | 'complete';

export function useScanner() {
  const [status, setStatus] = useState<ScanStatus>('idle');
  const [progress, setProgress] = useState(0);

  const startScan = () => {
    setStatus('aligning');
    setProgress(0);
  };

  useEffect(() => {
    if (status === 'aligning') {
      const timer = setTimeout(() => setStatus('scanning'), 2000);
      return () => clearTimeout(timer);
    }
    
    if (status === 'scanning') {
      const interval = setInterval(() => {
        setProgress((p) => {
          if (p >= 100) {
            clearInterval(interval);
            setStatus('analyzing');
            return 100;
          }
          return p + 2; // Increments over ~2-3 seconds
        });
      }, 50);
      return () => clearInterval(interval);
    }

    if (status === 'analyzing') {
      const timer = setTimeout(() => setStatus('complete'), 1500);
      return () => clearTimeout(timer);
    }
  }, [status]);

  return { status, progress, startScan, setStatus };
}
```
</action>
</task>

<task id="02-01-T3">
<title>Integrate webcam securely into Screen2</title>
<action>
Modify `puma-frontend/src/screens/Screen2FootScan/Screen2FootScan.tsx`. Wrap the `react-webcam` component in a clean container that covers the main view space. Add a button to manually trigger `startScan`.
</action>
</task>
